//! ix background daemon — ixd.
//!
//! Keeps the index fresh when the system is idle.

fn main() {
    println!("ixd: daemon is not yet fully implemented");
    println!("This binary would use watcher.rs and idle.rs to maintain the index.");
}
